# 安全策略

## 上报漏洞

**请勿在公开 issue 或讨论中披露安全漏洞。**

上报渠道（占位，由维护者填入真实渠道）：
`【TODO: 维护者邮箱 / 私密漏洞表单链接】`

请在报告中包含：影响的版本（`mindloop version`）、复现步骤、
影响评估。我们会在收到后尽快确认并同步修复进度；修复发布前请
勿公开披露细节。

## 安全模型（如实声明）

使用前请理解 mindloop 的边界——以下声明与代码行为一一对应：

1. **"沙箱"是进程树管辖，不是能力隔离。**
   Windows 下用 Job Object 实现（杀树/三类超时/输出上限），
   Linux/macOS 降级为进程级终止。模型生成的 bash 脚本以**当前用户
   的完整权限**运行——不要把不可信任务交给它，不要在含敏感凭据的
   环境里让它联网执行任意指令。

2. **仪表盘（`mindloop web`）默认只信任本机。**
   默认绑定 `127.0.0.1:8080`；`/api/*` 经同源守卫（跨源 Origin、
   回环部署下的非回环 Host 一律 403，防 CSRF 与 DNS rebinding）；
   写端点全部方法路由（GET 一律 405）。绑定非回环地址（如
   `0.0.0.0`）**必须**显式配置 Token（`--token` 或
   `MINDLOOP_WEB_TOKEN`），否则拒绝启动——此时请确保 Token 足够强，
   并优先考虑走 VPN/SSH 隧道而非公网暴露。

3. **密钥不落公开面。**
   `.env`（含 API key）被 .gitignore 排除，绝不入库；用量台账只记
   模型名与 token 计数，不记密钥。沙箱/子进程环境会剔除
   `MINDLOOP_API_KEY`、`ANTHROPIC_API_KEY`、`OPENAI_API_KEY`、
   `MINDLOOP_WEB_TOKEN` 等敏感变量。

4. **MCP 与技能是供应链面。**
   `skills install` 会执行 `git clone` 并落盘第三方目录；`mcp add`
   配置的命令会在本机启动。只安装与接入你信任的来源。
